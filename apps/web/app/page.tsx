import Image, { type ImageProps } from "next/image";
import { Button } from "@repo/ui/button";

export default function Home() {
	return (
		<div className="text-3xl">
			<main className={""}>new app</main>
			<footer className={""}></footer>
		</div>
	);
}
